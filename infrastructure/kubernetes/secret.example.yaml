apiVersion: v1
kind: Secret
metadata:
  name: stellar-pay-secrets
  namespace: stellar-pay
type: Opaque
stringData:
  # Replace with real values (base64 not required with stringData). Prefer
  # Azure Key Vault + Secrets Store CSI in production.
  JWT_SECRET: 'change-me'
  DATABASE_URL: 'postgresql://stellar_pay:change-me@postgres:5432/stellar_pay?schema=public'
  REDIS_URL: 'redis://redis:6379'
  WEBHOOK_SIGNING_SECRET: 'change-me'
  ADMIN_PASSWORD: 'ChangeMe123!'
