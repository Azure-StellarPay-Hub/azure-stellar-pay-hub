{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/admin && next build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --frozen-lockfile"
}
