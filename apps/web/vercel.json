{
  "framework": "nextjs",
  "buildCommand": "(cd .. && pnpm --filter @stellar-pay/ui --filter @stellar-pay/wallet build) && npx next build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --frozen-lockfile"
}
