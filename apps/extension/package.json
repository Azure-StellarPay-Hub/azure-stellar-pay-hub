{
  "name": "@stellar-pay/extension",
  "version": "0.1.0",
  "private": true,
  "description": "StellarPay Hub Chrome extension — payment companion with quick-send and notifications",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
