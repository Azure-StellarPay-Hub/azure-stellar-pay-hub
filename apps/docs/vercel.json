{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/docs && next build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --frozen-lockfile"
}
